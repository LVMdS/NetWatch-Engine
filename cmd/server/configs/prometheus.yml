global:
  scrape_interval: 15s # Raspa os alvos a cada 15 segundos

scrape_configs:
  - job_name: 'netwatch_backend'
    metrics_path: '/metrics'
    static_configs:
      # Aponta diretamente para o nome do contêiner Go na rede interna do Docker
      - targets: ['netwatch_go:8080']